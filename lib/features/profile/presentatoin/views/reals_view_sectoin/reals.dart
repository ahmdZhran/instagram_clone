import 'package:flutter/material.dart';

class RealsView extends StatelessWidget {
  const RealsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('REALS View created')),
    );
  }
}
