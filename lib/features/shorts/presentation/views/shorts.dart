import 'package:flutter/material.dart';

class ShortView extends StatelessWidget {
  const ShortView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Shorts videos')),
    );
  }
}
